export interface Comment {
    postId: number
    id: number
    name: string
    email: string
    body: boolean
}

export interface newComment {
    name: string
    email: string
    comment: string

}